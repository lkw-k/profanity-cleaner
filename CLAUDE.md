# 프로젝트 지침

## 코드 변경 후 자동 커밋 & 푸시

코드를 변경한 뒤에는 항상 커밋하고 원격에 푸시한다. 별도 요청 없이 자동으로 진행한다.

- 변경된 소스 파일만 스테이징 (`.claude/`, `*.db`, `.env` 등 제외)
- 커밋 메시지는 한국어, 변경 내용을 간결하게 요약
- `git push origin main`으로 즉시 푸시
