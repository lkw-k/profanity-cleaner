# 프로젝트 지침

## 코드 변경 후 자동 커밋 & 푸시

코드를 변경한 뒤에는 항상 커밋하고 원격에 푸시한다. 별도 요청 없이 자동으로 진행한다.

- 변경된 소스 파일만 스테이징 (`.claude/`, `*.db`, `.env` 등 제외)
- 커밋 메시지 형식: `ci: 변경 내용 한 줄 요약` (접두사 고정)
- `git push origin main`으로 즉시 푸시
- 커밋·푸시 성공은 별도 언급 안 함. 실패 시에만 알림

## 파일 데이터로 학습 시

사용자가 학습 데이터 파일을 제공하면:

- 파일을 파싱해 DB에 저장하거나 직접 학습에 사용한다
- 학습은 반드시 에폭별 진행 바(`_make_bar`)로 시각화한다
- 학습 완료 후 결과(데이터 수, 에폭, 손실값 등)를 출력한다
- 진행 상황은 항상 디스코드 메시지 편집 방식으로 실시간 표시한다
